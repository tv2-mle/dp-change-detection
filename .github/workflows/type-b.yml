# .github/workflows/type-b.yml
name: Type B Flow

on:
  workflow_call:
    inputs:
      folder_name:
        required: true
        type: string

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: |
          echo "hello im in type_b flows for ${{ inputs.folder_name }}"